{% extends 'base.html.twig' %}

{% block title %}Fiche{% endblock %}

{% block body %}
    <h1>Fiche</h1>

    <table class="table">
        <tbody>
            {% if fiche != null %}
            <tr>
                <th>AntecedentMaladie</th>
                <td>{{ fiche.antecedentMaladie }}</td>
            </tr>
            <tr>
                <th>AbitudeVie</th>
                <td>{{ fiche.abitudeVie }}</td>
            </tr>
            <tr>
                <th>HistoireMaladie</th>
                <td>{{ fiche.histoireMaladie }}</td>
            </tr>
            <tr>
                <th>Exploration</th>
                <td>{{ fiche.exploration }}</td>
            </tr>
            <tr>
                <th>Diagnostic</th>
                <td>{{ fiche.diagnostic }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ fiche.createdAt ? fiche.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
{#            <tr>#}
{#                <th>UpdatedAt</th>#}
{#                <td>{{ fiche.updatedAt ? fiche.updatedAt|date('Y-m-d H:i:s') : '' }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>DeletedAt</th>#}
{#                <td>{{ fiche.deletedAt ? fiche.deletedAt|date('Y-m-d H:i:s') : '' }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>IsDeleted</th>#}
{#                <td>{{ fiche.isDeleted ? 'Yes' : 'No' }}</td>#}
{#            </tr>#}
        {% endif %}
        </tbody>
    </table>

    {#<a href="{{ path('fiche_index') }}">back to list</a>#}
    {% if fiche != null %}
        <a type="button" class="btn btn-sm btn-warning" href="{{ path('fiche_edit', {'id': fiche.id}) }}">Modifier</a>
{#        <a href="{{ path('fiche_edit', {'id': fiche.id}) }}">edit</a>#}
        {{ include('fiche/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
