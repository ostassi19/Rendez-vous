{% extends 'base.html.twig' %}

{% block title %}Fiche{% endblock %}

{% block body %}
    <h1>Fiche</h1>

    <table class="table">
        <tbody>
            {% if fiche != null %}
            <tr>
                <th>AntecedentMaladie</th>
                <td>{{ fiche.antecedentMaladie }}</td>
            </tr>
            <tr>
                <th>AbitudeVie</th>
                <td>{{ fiche.abitudeVie }}</td>
            </tr>
            <tr>
                <th>HistoireMaladie</th>
                <td>{{ fiche.histoireMaladie }}</td>
            </tr>
            <tr>
                <th>Exploration</th>
                <td>{{ fiche.exploration }}</td>
            </tr>
            <tr>
                <th>Diagnostic</th>
                <td>{{ fiche.diagnostic }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ fiche.createdAt ? fiche.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>

        {% endif %}
        </tbody>
    </table>

    {#<a href="{{ path('fiche_index') }}">back to list</a>#}
    {% if fiche != null %}
        <a type="button" class="btn btn-primary mr-2" href="{{ path('fiche_edit', {'id': fiche.id}) }}">Modifier</a>
{#        <a href="{{ path('fiche_edit', {'id': fiche.id}) }}">edit</a>#}
{#        {{ include('fiche/_delete_form.html.twig') }}#}
    {% endif %}
{% endblock %}
